function openPopout(popoutId) {
    document.getElementById(popoutId).style.display = 'block';
}

function closePopout(popoutId) {
    document.getElementById(popoutId).style.display = 'none';
}
